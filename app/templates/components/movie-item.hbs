<div class="panel panel-default movie-item">
    <div class="panel-heading">
        <h3 class="panel-title">{{item.name}}</h3>
    </div>
    <div class="panel-body">
        <p>Year: {{item.year}}</p>
        <p>Review: {{item.review}}</p>
    </div>
    <div class="panel-footer text-right">
      {{yield}}
    </div>
</div>
